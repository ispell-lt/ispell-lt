var err = initInstall("Lietuvių kalbos rašybos tikrinimo žodynas", "lt@dictionaries.addons.mozilla.org", "@VERSION@");
if (err != SUCCESS)
    cancelInstall();

var fProgram = getFolder("Program");
err = addDirectory("", "lt@dictionaries.addons.mozilla.org", "dictionaries", fProgram, "dictionaries", true);
if (err != SUCCESS)
    cancelInstall();

performInstall();
